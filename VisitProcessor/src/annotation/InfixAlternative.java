package annotation;

public class InfixAlternative {

    public String toString() {
//        return "=" + tokens.stream().map(t).collect(Collectors.joining("|", "(", ")"));
        return null;
    }
}
